Marimo notebooks used for data analysis.
Bindle is an analog of binder for marimo notebooks: source notebooks are stored as `.py` files, executed via GitHub Actions, and exported to Jupyter `.ipynb` notebooks deployed to the `bindle` and `bindle-<notebook>` branches.

To edit a notebook locally, run `marimo edit <notebook>.py`.
